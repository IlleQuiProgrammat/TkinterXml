<Page>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="Submit" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="1" Text="Look, a label!" Padding="10" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
</Page>